# HUBINET tmux

set -g mouse on
set -g history-limit 200000

# Clipboard / OSC52
set -g set-clipboard on

# Zwykłe sesje zachowują standardowe zachowanie. Hubi ustawia remain-on-exit i
# window-size=largest wyłącznie w zarządzanych sesjach agentów (także hookiem
# dla ich nowych okien), bez zmiany innych sesji użytkownika.
setw -g remain-on-exit off

# Normalne zachowanie sesji
set -g exit-empty on
set -g detach-on-destroy on

# Numeracja
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# Terminal
set -g focus-events on

# Pasek
set -g status-interval 5
set -g status-left ' [#S] '
set -g status-right ' #H | %H:%M '

# Nowe okna/panele zachowują katalog
bind c new-window -c '#{pane_current_path}'
bind '"' split-window -v -c '#{pane_current_path}'
bind % split-window -h -c '#{pane_current_path}'
